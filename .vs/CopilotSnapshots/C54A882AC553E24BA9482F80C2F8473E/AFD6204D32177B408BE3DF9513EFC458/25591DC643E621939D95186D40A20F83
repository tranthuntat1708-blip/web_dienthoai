// src/pages/HomePage.jsx
import { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import { ArrowRight, ChevronLeft, ChevronRight } from 'lucide-react';
import ProductCard from '../components/product/ProductCard';
import FlashSaleCountdown from '../components/common/FlashSaleCountdown';
import { productApi } from '../api/products';

/* ── Dữ liệu tĩnh ───────────────────────────────────────── */
const CATEGORIES = [
  { name: 'Giỏ & Khay',    slug: 'gio-khay-dung-do',      img: 'https://placehold.co/120x120/e8d5bc/5c3d1e?text=🧺', count: 24 },
  { name: 'Đèn Mây Tre',   slug: 'den-may-tre-trang-tri',       img: 'https://placehold.co/120x120/e8d5bc/5c3d1e?text=🏮', count: 18 },
  { name: 'Đồ Nội Thất',   slug: 'do-noi-that-tu-nhien',        img: 'https://placehold.co/120x120/e8d5bc/5c3d1e?text=🪵', count: 32 },
  { name: 'Phụ Kiện TC',   slug: 'phu-kien-thoi-trang-thu-cong',img: 'https://placehold.co/120x120/e8d5bc/5c3d1e?text=👜', count: 15 },
  { name: 'Gương Decor',   slug: 'guong-trang-tri',       img: 'https://placehold.co/120x120/e8d5bc/5c3d1e?text=🪞', count: 12 },
  { name: 'Kệ & Giá',      slug: 'ke-gia-treo-decor',        img: 'https://placehold.co/120x120/e8d5bc/5c3d1e?text=🪵', count: 9 },
  { name: 'Trang Trí Tường',slug: 'trang-tri-tuong',     img: 'https://placehold.co/120x120/e8d5bc/5c3d1e?text=🖼️', count: 21 },
  { name: 'Lọ Hoa & Chậu', slug: 'lo-hoa-chau-cay-decor',       img: 'https://placehold.co/120x120/e8d5bc/5c3d1e?text=🪴', count: 17 },
  { name: 'Quà Tặng TC',   slug: 'qua-tang-thu-cong',           img: 'https://placehold.co/120x120/e8d5bc/5c3d1e?text=🎁', count: 14 },
  { name: 'Bộ Sưu Tập',    slug: 'bo-suu-tap',  img: 'https://placehold.co/120x120/e8d5bc/5c3d1e?text=✨', count: 6 },
];

const SALE_TABS = [
  { id: 'tab1', range: '01/06 - 30/06', label: '01/06 - 30/06' },
  { id: 'tab2', range: '10/06 - 30/12', label: '10/06 - 30/12' },
  { id: 'tab3', range: '01/07 - 30/04', label: '01/07 - 30/04' },
];

const VOUCHERS = [
  { code: 'CRAFT200',  label: 'Giảm 200k',    sub: 'Đơn từ 800k', color: '#d4460c' },
  { code: 'CRAF100',   label: 'Giảm 100k',    sub: 'Đơn từ 500k', color: '#c8872a' },
  { code: 'CRAFT50',   label: 'Giảm 50k',     sub: 'Đơn từ 300k', color: '#2d8a4e' },
  { code: 'FREESHIP',  label: 'Free Ship',     sub: 'Mọi đơn hàng', color: '#1e6ea8' },
];

const TRUSTED_BRANDS = [
  { name: 'VinHoms',   img: 'https://placehold.co/100x36/fff/999?text=VinHoms' },
  { name: 'Phú Đông',  img: 'https://placehold.co/100x36/fff/999?text=Phú+Đông' },
  { name: 'EcoPark',   img: 'https://placehold.co/100x36/fff/999?text=EcoPark' },
  { name: 'Nguyễn',    img: 'https://placehold.co/100x36/fff/999?text=Nguyễn' },
  { name: 'Celadon',   img: 'https://placehold.co/100x36/fff/999?text=Celadon' },
  { name: 'Cantavil',  img: 'https://placehold.co/100x36/fff/999?text=Cantavil' },
  { name: 'Masterise', img: 'https://placehold.co/100x36/fff/999?text=Masterise' },
];

const FLASH_SALE_END = new Date(Date.now() + 8 * 3600 * 1000).toISOString();

/* ── Section heading helper ──────────────────────────────── */
function SectionHeading({ label, title, subtitle }) {
  return (
    <div className="mb-6">
      {label && <p className="section-label">{label}</p>}
      <h2 className="section-title">{title}</h2>
      {subtitle && <p className="text-sm mt-1.5" style={{ color: 'var(--craft-muted)' }}>{subtitle}</p>}
    </div>
  );
}

/* ── Category row with slider ────────────────────────────── */
function CategoryRow({ items, title, productList, large = false }) {
  return (
    <section className="max-w-7xl mx-auto px-4 py-10">
  <div className={`flex flex-col ${large ? 'lg:flex-row-reverse' : 'lg:flex-row'} gap-6 items-start`}>

        {/* Left: hero image */}
        <div className={`${large ? 'lg:w-80' : 'lg:w-72'} flex-shrink-0`}>
 <div className="rounded-3xl overflow-hidden aspect-[4/5]" style={{ backgroundColor: 'var(--craft-beige)' }}>
            <img src={`https://placehold.co/320x400/e8d5bc/5c3d1e?text=${encodeURIComponent(title)}`}
     alt={title} className="w-full h-full object-cover" />
          </div>
        </div>

     {/* Right: info + products */}
  <div className="flex-1 min-w-0">
          <div className="flex items-end justify-between mb-5">
            <div>
       <p className="section-label">{label}</p>
<h2 className="section-title">{title}</h2>
    <p className="text-sm mt-1" style={{ color: 'var(--craft-muted)' }}>{items}</p>
     </div>
  <div className="flex gap-2">
      <button className="w-8 h-8 rounded-full border flex items-center justify-center hover:bg-amber-50 transition-colors"
 style={{ borderColor: 'var(--craft-tan)' }}>
                <ChevronLeft size={16} style={{ color: 'var(--craft-brown)' }} />
      </button>
    <button className="w-8 h-8 rounded-full border flex items-center justify-center hover:bg-amber-50 transition-colors"
       style={{ borderColor: 'var(--craft-tan)' }}>
     <ChevronRight size={16} style={{ color: 'var(--craft-brown)' }} />
              </button>
            </div>
          </div>

       <div className="grid grid-cols-2 md:grid-cols-3 gap-3">
    {productList.slice(0, 3).map(p => <ProductCard key={p.id} product={p} />)}
        </div>

          <div className="mt-4">
     <Link to="/danh-muc" className="btn-outline text-sm px-5 py-2">
     Xem tất cả →
  </Link>
          </div>
        </div>
      </div>
    </section>
  );
}

/* ═══════════════════════════════════════════════════════════ */
export default function HomePage() {
  const [featured,          setFeatured]     = useState([]);
  const [flashProducts,     setFlashProducts]     = useState([]);
  const [loading,           setLoading]  = useState(true);
  const [activeSaleTab,     setActiveSaleTab]     = useState('tab3');
  const [heroSlide,         setHeroSlide]  = useState(0);

  const heroSlides = [
    {
      tag: 'TINH HOA',
      title: 'MÂY TRE ĐAN',
      discount: '50%',
      sub: 'Sản phẩm đồ dùng trang trí nội thất bằng mây tre lá\nthiên nhiên – Giảm giá lên đến 01/06 – 25/12.',
      bg: '#f0e6d4',
      accent: 'var(--craft-amber)',
    },
    {
      tag: 'BỘ SƯU TẬP',
    title: 'ĐÈN TRANG TRÍ',
      discount: '30%',
      sub: 'Đèn mây tre trang trí không gian sống – Phong cách Indochine độc đáo.',
      bg: '#e8f0d4',
      accent: '#4a7c40',
    },
  ];

  useEffect(() => {
    productApi.getProducts({ page: 1, pageSize: 8, sortBy: 'best_seller' })
      .then(res => { setFeatured(res.items); setLoading(false); })
      .catch(() => setLoading(false));

    productApi.getProducts({ page: 1, pageSize: 4, sortBy: 'newest' })
      .then(res => setFlashProducts(res.items))
  .catch(() => {});
  }, []);

  const hero = heroSlides[heroSlide];

  return (
    <div style={{ backgroundColor: 'var(--craft-cream)' }}>

      {/* ════════════════════════════════════════════════
          1. HERO BANNER
      ════════════════════════════════════════════════ */}
   <section className="relative overflow-hidden" style={{ backgroundColor: hero.bg, minHeight: 360 }}>
        <div className="max-w-7xl mx-auto px-4 py-10 md:py-16 flex flex-col md:flex-row items-center gap-8">

          {/* Text */}
          <div className="flex-1 space-y-4 animate-fadeInUp">
     <span className="inline-block text-xs font-bold uppercase tracking-widest px-3 py-1 rounded-full text-white"
    style={{ backgroundColor: hero.accent }}>{hero.tag}</span>
     <h1 className="text-5xl md:text-7xl font-black leading-none tracking-tight"
        style={{ fontFamily: "'Playfair Display',serif", color: 'var(--craft-brown)' }}>
          {hero.title}
            </h1>
        <div className="inline-flex items-center gap-3">
      <span className="text-5xl font-black" style={{ color: 'var(--craft-amber)' }}>GIẢM SỐC</span>
   <span className="text-6xl font-black text-white px-4 py-1 rounded-2xl"
        style={{ backgroundColor: hero.accent }}>{hero.discount}</span>
      </div>
            <p className="text-sm leading-relaxed max-w-sm whitespace-pre-line"
     style={{ color: 'var(--craft-muted)' }}>{hero.sub}</p>
 <div className="flex gap-3 pt-2">
            <Link to="/danh-muc" className="btn-primary flex items-center gap-2 px-7 py-3 text-sm">
                Khám phá ngay <ArrowRight size={16} />
      </Link>
              <Link to="/dat-lich" className="btn-outline px-7 py-3 text-sm">Tư vấn miễn phí</Link>
            </div>
     </div>

          {/* Hero image */}
          <div className="flex-1 flex justify-center">
       <div className="relative w-72 h-72 md:w-96 md:h-96">
    <div className="absolute inset-4 rounded-full opacity-40 blur-2xl"
                style={{ backgroundColor: hero.accent }} />
     <img src="https://placehold.co/420x420/d4a574/fff?text=🧺"
 alt="Hero" className="relative rounded-3xl w-full h-full object-cover shadow-2xl" />
  {/* Sale badge overlay */}
              <div className="absolute bottom-4 right-4 bg-white rounded-2xl px-4 py-2 shadow-lg text-center">
  <p className="text-xs font-semibold" style={{ color: 'var(--craft-muted)' }}>Giảm đến</p>
      <p className="text-2xl font-black" style={{ color: 'var(--craft-amber)' }}>{hero.discount}</p>
      </div>
      </div>
          </div>
        </div>

        {/* Slide dots */}
        <div className="absolute bottom-4 left-1/2 -translate-x-1/2 flex gap-2">
      {heroSlides.map((_, i) => (
         <button key={i} onClick={() => setHeroSlide(i)}
              className="h-1.5 rounded-full transition-all"
       style={{ width: i === heroSlide ? 24 : 8, backgroundColor: i === heroSlide ? hero.accent : '#c8b09a' }} />
          ))}
        </div>
    </section>

      {/* ════════════════════════════════════════════════
          2. DANH MỤC – icon tròn
  ════════════════════════════════════════════════ */}
<section className="max-w-7xl mx-auto px-4 py-10">
     <div className="flex items-center justify-between mb-5">
        <div />
       <div className="flex gap-1.5">
        <button className="w-7 h-7 rounded-full border flex items-center justify-center"
      style={{ borderColor: 'var(--craft-tan)' }}><ChevronLeft size={14} /></button>
       <button className="w-7 h-7 rounded-full border flex items-center justify-center"
      style={{ borderColor: 'var(--craft-tan)' }}><ChevronRight size={14} /></button>
        </div>
        </div>
        <div className="grid grid-cols-5 md:grid-cols-10 gap-3">
          {CATEGORIES.map(cat => (
     <Link key={cat.slug} to={`/danh-muc?category=${cat.slug}`}
   className="flex flex-col items-center gap-2 group">
   <div className="w-14 h-14 md:w-16 md:h-16 rounded-full overflow-hidden border-2 transition-all group-hover:scale-110"
              style={{ borderColor: 'var(--craft-tan)', backgroundColor: 'var(--craft-beige)' }}>
      <img src={cat.img} alt={cat.name} className="w-full h-full object-cover" />
  </div>
     <span className="text-[10px] text-center font-medium leading-tight"
       style={{ color: 'var(--craft-brown)' }}>{cat.name}</span>
          <span className="text-[9px]" style={{ color: 'var(--craft-muted)' }}>{cat.count} sp</span>
      </Link>
      ))}
        </div>
      </section>

      {/* ════════════════════════════════════════════════
3. TESTIMONIALS
      ════════════════════════════════════════════════ */}
      <section style={{ backgroundColor: 'var(--craft-brown)' }} className="py-12">
        <div className="max-w-5xl mx-auto px-4 flex flex-col md:flex-row items-center gap-10">
    <div className="flex-1 space-y-4 text-center md:text-left">
    <p className="section-label text-amber-400" style={{ color: '#f59e0b' }}>• THỦ CÔNG CRAFT</p>
         <h2 className="text-3xl font-bold text-white" style={{ fontFamily: "'Playfair Display',serif" }}>
              Khách hàng nói về chúng tôi
            </h2>
            <p className="text-amber-200 text-sm">Hơn 20.000 khách hàng đã sử dụng sản phẩm của chúng tôi</p>
          </div>
<div className="flex-1 bg-white/10 backdrop-blur rounded-3xl p-6 space-y-3">
            <div className="flex items-center gap-3">
       <img src="https://placehold.co/40x40/e8d5bc/5c3d1e?text=K"
             className="w-10 h-10 rounded-full object-cover" alt="avatar" />
    <div>
                <p className="font-semibold text-white text-sm">Ngân Vy</p>
          <div className="flex gap-0.5">
   {Array.from({ length: 5 }).map((_, i) => (
   <span key={i} className="text-amber-400 text-xs">★</span>
           ))}
</div>
       </div>
   </div>
 <p className="text-sm text-amber-100 leading-relaxed">
    "Tôi thật sự bị cuốn vào các sản phẩm của Sudes Craft. Từng đường nét thủ công tinh xảo, chất liệu và câu chuyện của người thợ. Đặc biệt là mỗi sản phẩm làm mỗi lần, mỗi mẫu mỗi khác nhau."
            </p>
      {/* Dots */}
   <div className="flex gap-1.5 pt-1">
    {[0, 1, 2].map(i => (
     <span key={i} className="h-1.5 rounded-full"
        style={{ width: i === 0 ? 20 : 8, backgroundColor: i === 0 ? '#f59e0b' : 'rgba(255,255,255,0.3)' }} />
  ))}
   </div>
          </div>
        </div>

        {/* Brand logos */}
        <div className="max-w-5xl mx-auto px-4 mt-8">
      <div className="flex items-center justify-around flex-wrap gap-4">
            {TRUSTED_BRANDS.map(b => (
  <img key={b.name} src={b.img} alt={b.name} className="h-8 object-contain opacity-70 hover:opacity-100 transition-opacity" />
            ))}
          </div>
      </div>
      </section>

      {/* ════════════════════════════════════════════════
   4. YEAR END SALE – Flash Sale
      ════════════════════════════════════════════════ */}
      <section className="max-w-7xl mx-auto px-4 py-12">
        <div className="rounded-3xl overflow-hidden" style={{ backgroundColor: '#fff8ee', border: '1.5px solid var(--craft-tan)' }}>
          <div className="p-6 md:p-8">
    <div className="text-center mb-6">
          <p className="section-label justify-center">CÙNG SUDES CRAFT KHỞI ĐỘNG</p>
              <h2 className="section-title text-center text-3xl">
            "Year End Craft Sale – Xả Kho Cuối Năm"
          </h2>
         <p className="text-sm mt-2" style={{ color: 'var(--craft-muted)' }}>
         Các sản phẩm đồ thủ công với giá khủng – Giảm sâu nhiều sản phẩm từ 01/06 – 25/12.
              </p>
            </div>

       {/* Tabs + Countdown */}
            <div className="flex flex-wrap items-center justify-between gap-4 mb-6">
              <div className="flex gap-2 flex-wrap">
     {SALE_TABS.map(tab => (
      <button key={tab.id} onClick={() => setActiveSaleTab(tab.id)}
     className="text-xs font-semibold px-4 py-2 rounded-full transition-all"
          style={{
     backgroundColor: activeSaleTab === tab.id ? 'var(--craft-amber)' : 'transparent',
            color: activeSaleTab === tab.id ? '#fff' : 'var(--craft-brown)',
     border: `1.5px solid ${activeSaleTab === tab.id ? 'var(--craft-amber)' : 'var(--craft-tan)'}`,
  }}>
    {tab.label}
        </button>
        ))}
          </div>

     <div className="flex items-center gap-3">
                <span className="text-xs font-medium" style={{ color: 'var(--craft-muted)' }}>Kết thúc sau:</span>
      <FlashSaleCountdown endAt={FLASH_SALE_END} />
         <div className="flex items-center gap-1.5 ml-4">
          <button className="text-xs px-3 py-1.5 rounded-full border font-medium"
              style={{ borderColor: 'var(--craft-tan)', color: 'var(--craft-brown)' }}>19</button>
       <button className="text-xs px-3 py-1.5 rounded-full font-medium text-white"
         style={{ backgroundColor: 'var(--craft-amber)' }}>21</button>
  <button className="text-xs px-3 py-1.5 rounded-full border font-medium"
              style={{ borderColor: 'var(--craft-tan)', color: 'var(--craft-brown)' }}>32</button>
          </div>
       </div>
            </div>

            {/* Flash sale products */}
    <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
    {flashProducts.length > 0
                ? flashProducts.map(p => <ProductCard key={p.id} product={{ ...p, isOnSale: true }} />)
            : Array.from({ length: 4 }).map((_, i) => (
          <div key={i} className="aspect-square skeleton" />
   ))
          }
     </div>
   </div>
        </div>
      </section>

{/* ════════════════════════════════════════════════
          5. VOUCHER ROW
      ════════════════════════════════════════════════ */}
      <section className="max-w-7xl mx-auto px-4 pb-10">
        <SectionHeading label="THỦ CÔNG SUDES CRAFT" title="Mã giảm giá dành cho bạn" />
        <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
          {VOUCHERS.map(v => (
  <div key={v.code} className="flex items-center gap-3 bg-white rounded-2xl p-4 border"
  style={{ borderColor: 'var(--craft-tan)' }}>
            <div className="w-10 h-10 rounded-full flex items-center justify-center flex-shrink-0"
     style={{ backgroundColor: v.color }}>
  <span className="text-white font-black text-base">%</span>
    </div>
         <div className="min-w-0">
  <p className="font-bold text-sm font-mono" style={{ color: v.color }}>{v.code}</p>
      <p className="text-xs font-semibold" style={{ color: 'var(--craft-brown)' }}>{v.label}</p>
            <p className="text-[10px]" style={{ color: 'var(--craft-muted)' }}>{v.sub}</p>
              </div>
            </div>
  ))}
        </div>
      </section>

   {/* ════════════════════════════════════════════════
          6. SẢN PHẨM NỔI BẬT
      ════════════════════════════════════════════════ */}
      <section className="max-w-7xl mx-auto px-4 pb-14">
        <div className="flex items-end justify-between mb-6">
          <SectionHeading label="THỦ CÔNG SUDES CRAFT" title="Các sản phẩm nổi bật" subtitle="Sự kết hợp hài hoà giữa thủ công truyền thống và thiết kế hiện đại – đậm chất tự nhiên và bền vững." />
   <div className="flex gap-2 mb-6">
        <button className="w-9 h-9 rounded-full border flex items-center justify-center hover:bg-amber-50 transition-colors"
 style={{ borderColor: 'var(--craft-tan)' }}><ChevronLeft size={16} /></button>
        <button className="w-9 h-9 rounded-full border flex items-center justify-center hover:bg-amber-50 transition-colors"
              style={{ borderColor: 'var(--craft-tan)' }}><ChevronRight size={16} /></button>
      </div>
      </div>
        {loading ? (
          <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
   {Array.from({ length: 8 }).map((_, i) => <div key={i} className="aspect-square skeleton" />)}
          </div>
        ) : (
          <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
            {featured.map(p => <ProductCard key={p.id} product={p} />)}
      </div>
        )}
 <div className="text-center mt-8">
     <Link to="/danh-muc" className="btn-outline px-10 py-3 text-sm">Xem thêm →</Link>
      </div>
 </section>

      {/* ════════════════════════════════════════════════
          7. SECTION DANH MỤC – Giỏ & Khay (left image)
      ════════════════════════════════════════════════ */}
      <div style={{ backgroundColor: 'var(--craft-beige)' }}>
    <section className="max-w-7xl mx-auto px-4 py-12">
          <div className="flex flex-col lg:flex-row gap-8 items-start">
        {/* Hero image LEFT */}
<div className="lg:w-80 flex-shrink-0">
    <div className="rounded-3xl overflow-hidden" style={{ aspectRatio: '4/5' }}>
           <img src="https://placehold.co/320x400/d4a574/fff?text=Giỏ+Khay"
         alt="Giỏ & Khay" className="w-full h-full object-cover" />
              </div>
    </div>
        {/* Content RIGHT */}
        <div className="flex-1">
 <div className="flex items-end justify-between mb-5">
    <div>
          <p className="section-label">THỦ CÔNG SUDES CRAFT</p>
        <h2 className="section-title">Giỏ & Khay Đan Tay</h2>
     <p className="text-sm mt-1" style={{ color: 'var(--craft-muted)' }}>
        Từng nét đan thủ công, từu giỏ xe đến vật đựng và trang trí cho không gian sống.
       </p>
     </div>
 <div className="flex gap-2 mb-1">
           <button className="w-8 h-8 rounded-full border flex items-center justify-center"
      style={{ borderColor: 'var(--craft-tan)' }}><ChevronLeft size={14} /></button>
      <button className="w-8 h-8 rounded-full border flex items-center justify-center"
       style={{ borderColor: 'var(--craft-tan)' }}><ChevronRight size={14} /></button>
         </div>
         </div>
      <div className="grid grid-cols-2 md:grid-cols-3 gap-3">
         {(featured.slice(0, 3).length > 0 ? featured.slice(0, 3) : Array.from({ length: 3 }).map((_, i) => ({ id: i, isPlaceholder: true }))).map((p, i) =>
         p.isPlaceholder
      ? <div key={i} className="aspect-square skeleton" />
        : <ProductCard key={p.id} product={p} />
      )}
   </div>
 <div className="mt-5">
           <Link to="/danh-muc?category=gio-khay-dung-do"
      className="btn-outline text-sm px-6 py-2.5">Xem tất cả →</Link>
  </div>
   </div>
  </div>
        </section>
      </div>

      {/* ════════════════════════════════════════════════
          8. SECTION DANH MỤC – Đèn Mây Tre (right image)
      ════════════════════════════════════════════════ */}
      <section className="max-w-7xl mx-auto px-4 py-12">
        <div className="flex flex-col lg:flex-row-reverse gap-8 items-start">
{/* Hero image RIGHT */}
     <div className="lg:w-80 flex-shrink-0">
            <div className="rounded-3xl overflow-hidden" style={{ aspectRatio: '4/5' }}>
           <img src="https://placehold.co/320x400/c8872a/fff?text=Đèn+Mây+Tre"
        alt="Đèn Mây Tre" className="w-full h-full object-cover" />
   </div>
          </div>
          {/* Content LEFT */}
        <div className="flex-1">
      <div className="flex items-end justify-between mb-5">
        <div>
             <p className="section-label">THỦ CÔNG SUDES CRAFT</p>
          <h2 className="section-title">Đèn Mây Tre Trang Trí</h2>
     <p className="text-sm mt-1" style={{ color: 'var(--craft-muted)' }}>
       Ánh sáng lan qua từng nét đan, mang thêm hơi thở tự nhiên vào không gian sống.
</p>
           </div>
        <div className="flex gap-2 mb-1">
      <button className="w-8 h-8 rounded-full border flex items-center justify-center"
       style={{ borderColor: 'var(--craft-tan)' }}><ChevronLeft size={14} /></button>
             <button className="w-8 h-8 rounded-full border flex items-center justify-center"
        style={{ borderColor: 'var(--craft-tan)' }}><ChevronRight size={14} /></button>
              </div>
            </div>
            <div className="grid grid-cols-2 md:grid-cols-3 gap-3">
    {(featured.slice(2, 5).length > 0 ? featured.slice(2, 5) : Array.from({ length: 3 }).map((_, i) => ({ id: i, isPlaceholder: true }))).map((p, i) =>
        p.isPlaceholder
        ? <div key={i} className="aspect-square skeleton" />
       : <ProductCard key={p.id} product={p} />
         )}
     </div>
            <div className="mt-5">
        <Link to="/danh-muc?category=den-may-tre-trang-tri"
                className="btn-outline text-sm px-6 py-2.5">Xem tất cả →</Link>
     </div>
    </div>
        </div>
  </section>

      {/* ════════════════════════════════════════════════
    9. STORY BANNER – Câu chuyện về chúng tôi
      ════════════════════════════════════════════════ */}
      <section className="py-14" style={{ backgroundColor: '#f5ede0' }}>
        <div className="max-w-7xl mx-auto px-4 flex flex-col lg:flex-row gap-8 items-center">
          {/* Story image */}
          <div className="lg:w-2/5 flex-shrink-0">
    <div className="rounded-3xl overflow-hidden aspect-video lg:aspect-[4/3]">
     <img src="https://placehold.co/560x420/a87040/fff?text=Nghề+Thủ+Công"
     alt="Story" className="w-full h-full object-cover" />
            </div>
          </div>
  {/* Story text */}
       <div className="flex-1 space-y-4">
      <p className="section-label">THỦ CÔNG SUDES CRAFT</p>
<h2 className="section-title text-3xl md:text-4xl">Câu chuyện về Sudes Craft</h2>
     <p className="text-sm leading-relaxed" style={{ color: 'var(--craft-muted)' }}>
  Lấy cảm hứng từ chất liệu mây, tre, gỗ, tre/tre cỏ biển. Sudes Craft cam kết và đẹp những câu chuyện bảo nghề truyền từ Việt. Mỗi sản phẩm là cầu nối giữa người thợ truyền thống và người yêu thủ công nghệ thuật, mang đến cho bạn không gian sống sang, biến vừng và đậy cảm xúc.
    </p>
  <div className="grid grid-cols-3 gap-3">
              {['https://placehold.co/180x130/c8a07a/fff?text=Thủ+Công+1',
                'https://placehold.co/180x130/a87040/fff?text=Thủ+Công+2',
         'https://placehold.co/180x130/8a6030/fff?text=Thủ+Công+3'].map((img, i) => (
    <div key={i} className="rounded-2xl overflow-hidden aspect-[4/3]">
     <img src={img} alt="" className="w-full h-full object-cover hover:scale-105 transition-transform duration-300" />
  </div>
           ))}
            </div>
      <Link to="/blog" className="btn-primary inline-flex items-center gap-2 px-7">
              Xem chi tiết <ArrowRight size={16} />
    </Link>
  </div>
  </div>
      </section>

      {/* ════════════════════════════════════════════════
   10. TikTok Section
      ════════════════════════════════════════════════ */}
      <section className="py-12" style={{ backgroundColor: 'var(--craft-brown)' }}>
        <div className="max-w-7xl mx-auto px-4 flex flex-col lg:flex-row gap-10 items-center">
          <div className="flex-1 space-y-4">
   <p className="text-amber-400 text-xs font-bold uppercase tracking-widest">• THỦ CÔNG SUDES CRAFT</p>
            <h2 className="text-3xl font-bold text-white" style={{ fontFamily: "'Playfair Display',serif" }}>
           Đón nguồn cảm hứng mộc mạc cùng Sudes Craft trên TikTok
            </h2>
            <p className="text-amber-200 text-sm leading-relaxed max-w-md">
     Những khoảnh khắc tỉ mỉ khi làm nên những món đồ quen thuộc — từ việc đan mây, tre đến gắn mộ, khêu phô chất. Sudes Craft mang tới thiên nghề thủ công đến cuộc sống hàng ngày qua những video decor tỉ mỉ về phong cách sống giản đơn đầy cảm xúc.
            </p>
      <a href="#" className="btn-primary inline-flex items-center gap-2 px-7">
          Theo dõi ngay <ArrowRight size={16} />
        </a>
        </div>
        {/* TikTok embed preview */}
      <div className="lg:w-2/5">
            <div className="rounded-3xl overflow-hidden bg-white p-4 shadow-2xl">
     <div className="flex items-center gap-2 mb-3">
         <div className="w-8 h-8 bg-black rounded-lg flex items-center justify-center">
        <span className="text-white font-black text-sm">♪</span>
    </div>
      <span className="font-semibold text-sm text-gray-800">TikTok</span>
     <span className="text-xs text-gray-400 ml-auto">85 đang theo dõi</span>
              </div>
      <div className="grid grid-cols-3 gap-1.5">
         {['3,217', '5,634', '11,023'].map((count, i) => (
         <div key={i} className="relative aspect-[3/4] rounded-xl overflow-hidden">
  <img src={`https://placehold.co/120x160/c8a07a/fff?text=Video+${i+1}`}
   alt="" className="w-full h-full object-cover" />
 <span className="absolute bottom-1.5 left-1.5 text-[10px] text-white font-bold drop-shadow">{count}</span>
        </div>
              ))}
        </div>
 <div className="mt-3 flex justify-end">
      <span className="text-xs text-gray-500">TikTok · </span>
    <a href="#" className="text-xs font-semibold ml-1" style={{ color: 'var(--craft-amber)' }}>Mở TikTok</a>
              </div>
    </div>
  </div>
        </div>
      </section>

  {/* ════════════════════════════════════════════════
          11. TIN TỨC MỚI NHẤT
      ════════════════════════════════════════════════ */}
      <section className="max-w-7xl mx-auto px-4 py-14">
  <div className="flex items-end justify-between mb-6">
          <SectionHeading label="THỦ CÔNG SUDES CRAFT" title="Tin tức mới nhất từ Sudes Craft" />
     <div className="flex gap-2 mb-6">
            <button className="w-9 h-9 rounded-full border flex items-center justify-center"
              style={{ borderColor: 'var(--craft-tan)' }}><ChevronLeft size={16} /></button>
          <button className="w-9 h-9 rounded-full border flex items-center justify-center"
      style={{ borderColor: 'var(--craft-tan)' }}><ChevronRight size={16} /></button>
        </div>
        </div>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-5">
  {[
 { tag: 'LIFESTYLE', date: '17 OCT', title: 'Mây Tre Đan – Hướng Thời Trang Thiên Nhiên Thân Thiện Môi Trường', img: 'https://placehold.co/380x240/d4a574/fff?text=Blog+1' },
       { tag: 'DESIGN', date: '17 OCT', title: 'Giỏ Mây Tre Đan Cỏ Nẻo – Món Nội Thất Tiện Ích Cho Không Gian Sống Con Bản', img: 'https://placehold.co/380x240/c8872a/fff?text=Blog+2' },
       { tag: 'DESIGN', date: '17 OCT', title: 'Ý Tưởng Sáng Tạo Trang Trí Không Gian Sống Với Sản Phẩm Mây Tre Thủ Công', img: 'https://placehold.co/380x240/a87040/fff?text=Blog+3' },
          ].map(post => (
            <Link key={post.title} to="/blog"
       className="bg-white rounded-2xl overflow-hidden border transition-all hover:shadow-lg hover:-translate-y-1"
           style={{ borderColor: 'var(--craft-tan)' }}>
      <div className="aspect-video overflow-hidden">
   <img src={post.img} alt={post.title} className="w-full h-full object-cover hover:scale-105 transition-transform duration-300" />
     </div>
              <div className="p-4 space-y-2">
         <div className="flex items-center gap-2">
      <span className="text-[10px] font-bold px-2 py-0.5 rounded-full text-white"
        style={{ backgroundColor: 'var(--craft-amber)' }}>{post.tag}</span>
            <span className="text-xs" style={{ color: 'var(--craft-muted)' }}>{post.date}</span>
           </div>
<h3 className="text-sm font-semibold line-clamp-2 leading-snug"
      style={{ color: 'var(--craft-brown)' }}>{post.title}</h3>
       <span className="btn-ghost text-xs">Xem thêm →</span>
         </div>
      </Link>
        ))}
 </div>
      <div className="text-center mt-8">
          <Link to="/blog" className="btn-outline px-10 py-3 text-sm">Xem thêm</Link>
     </div>
      </section>

      {/* ════════════════════════════════════════════════
      12. SERVICE BADGES
      ════════════════════════════════════════════════ */}
      <div style={{ borderTop: '1.5px solid var(--craft-tan)', borderBottom: '1.5px solid var(--craft-tan)' }}>
   <div className="max-w-7xl mx-auto px-4 py-6 grid grid-cols-2 md:grid-cols-4 gap-4">
    {[
   { icon: '🚚', title: 'Giao hàng tận nơi', sub: 'Miễn phí, nhanh chóng, an toàn, trên toàn quốc' },
            { icon: '🔄', title: 'Đổi 1 – 1', sub: 'Miễn phí đổi trả trong 30 ngày kể từ khi nhận hàng' },
            { icon: '🛡️', title: 'Bảo hành chu đáo', sub: 'Bảo hành, chăm chút, dịch vụ sau bán hàng tận tâm' },
          { icon: '💬', title: 'Tư vấn decor', sub: 'Tư vấn phong thủy thiết kế theo phong cách của bạn' },
     ].map(s => (
            <div key={s.title} className="flex items-start gap-3">
      <span className="text-2xl flex-shrink-0">{s.icon}</span>
   <div>
    <p className="font-semibold text-sm" style={{ color: 'var(--craft-brown)' }}>{s.title}</p>
            <p className="text-xs leading-relaxed" style={{ color: 'var(--craft-muted)' }}>{s.sub}</p>
       </div>
            </div>
          ))}
        </div>
    </div>
    </div>
  );
}
