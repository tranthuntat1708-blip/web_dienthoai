// src/components/layout/Footer.jsx
import { Link } from 'react-router-dom';
import { MapPin, Phone, Mail, Facebook, Instagram, Youtube } from 'lucide-react';

const socialLinks = [
  { icon: Facebook, href: '#' },
  { icon: Instagram, href: '#' },
  { icon: Youtube, href: '#' },
];

const supportLinks = [
  { to: '/dat-lich',             label: 'Liên hệ đặt lịch' },
  { to: '#',    label: 'Hỏi đáp' },
  { to: '#',   label: 'Chính sách đổi trả' },
  { to: '#',              label: 'Hướng dẫn chuyển khoản' },
  { to: '/tai-khoan/don-hang',   label: 'Tra cứu đơn hàng' },
  { to: '#',   label: 'Cảm ơn khách hàng' },
];

export default function Footer() {
  return (
    <footer style={{ backgroundColor: '#1e1208', color: '#c8b09a' }}>

   {/* ── Main footer ─────────────────────────────────── */}
      <div className="max-w-7xl mx-auto px-4 py-12 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-8">

  {/* Brand */}
        <div className="lg:col-span-2 space-y-4">
          <div className="flex items-center gap-3">
     <div className="w-10 h-10 rounded-full flex items-center justify-center text-xl"
    style={{ backgroundColor: 'var(--craft-tan)' }}>🧺</div>
      <div>
         <span className="block font-bold text-white text-base"
    style={{ fontFamily: "'Playfair Display',serif" }}>Nội Thất Craft</span>
              <span className="block text-[10px] uppercase tracking-widest"
   style={{ color: 'var(--craft-amber)' }}>Thủ Công · Tự Nhiên · Bền Vững</span>
      </div>
     </div>
  <p className="text-sm leading-relaxed max-w-xs" style={{ color: '#a09080' }}>
    Sudes Craft – thương hiệu decor thủ công uy tín tại Việt Nam. Chúng tôi cung cấp sản phẩm mây tre lá, đồ thủ công mỹ nghệ chất lượng cao và câu chuyện sản phẩm độc đáo.
          </p>
          <div className="space-y-1.5 text-sm" style={{ color: '#a09080' }}>
  <div className="flex items-start gap-2">
          <MapPin size={14} className="mt-0.5 flex-shrink-0" style={{ color: 'var(--craft-amber)' }} />
      <span>70 Lữ Gia, Phường Tân Thới, Q.11, TP. HCM</span>
     </div>
            <div className="flex items-center gap-2">
    <Phone size={14} style={{ color: 'var(--craft-amber)' }} />
    <span>1900 2345</span>
     </div>
    <div className="flex items-center gap-2">
    <Mail size={14} style={{ color: 'var(--craft-amber)' }} />
      <span>support@sudescraft.vn</span>
   </div>
      </div>
          <div className="flex gap-2.5 pt-1">
            {socialLinks.map(({ icon: Icon, href }) => (
        <a key={href + Icon.displayName} href={href}
          className="w-9 h-9 rounded-full border flex items-center justify-center transition-colors hover:bg-amber-600 hover:border-amber-600"
  style={{ borderColor: '#4a3020', color: '#a09080' }}>
    <Icon size={15} />
      </a>
            ))}
  <a href="#"
     className="w-9 h-9 rounded-full border flex items-center justify-center transition-colors hover:bg-amber-600 hover:border-amber-600"
              style={{ borderColor: '#4a3020', color: '#a09080' }}>
              <span className="text-sm font-black">♪</span>
            </a>
          </div>
        </div>

        {/* Về chúng tôi */}
   <div>
  <h4 className="text-white font-semibold mb-4 text-sm">VỀ CHÚNG TÔI</h4>
 <ul className="space-y-2.5 text-sm" style={{ color: '#a09080' }}>
      {['Hệ thống cửa hàng', 'Hệ thống đại lý', 'Hướng dẫn mua hàng', 'Quy định hoàn tiền'].map(l => (
    <li key={l}><Link to="#" className="hover:text-amber-400 transition-colors">{l}</Link></li>
            ))}
          </ul>
        </div>

        {/* Hỗ trợ */}
   <div>
<h4 className="text-white font-semibold mb-4 text-sm">HỖ TRỢ</h4>
          <ul className="space-y-2.5 text-sm" style={{ color: '#a09080' }}>
    {supportLinks.map(l => (
     <li key={l.label}>
           <Link to={l.to} className="hover:text-amber-400 transition-colors">{l.label}</Link>
              </li>
       ))}
      </ul>
        </div>

{/* Đăng ký + Thanh toán */}
      <div className="space-y-5">
  <div>
      <h4 className="text-white font-semibold mb-3 text-sm">ĐĂNG KÝ NHẬN TIN</h4>
      <p className="text-xs mb-3" style={{ color: '#a09080' }}>Nhận thông tin sản phẩm mới, khuyến mãi hot và các chương trình ưu đãi.</p>
        <div className="flex">
         <input placeholder="Nhập email của bạn..."
        className="flex-1 text-xs px-3 py-2.5 rounded-l-lg border-0 focus:outline-none"
      style={{ backgroundColor: '#2d1f0e', color: '#c8b09a' }} />
       <button className="px-3 py-2.5 rounded-r-lg text-white flex-shrink-0"
                style={{ backgroundColor: 'var(--craft-amber)' }}>
  <Mail size={15} />
            </button>
        </div>
 </div>

       <div>
            <h4 className="text-white font-semibold mb-3 text-sm">HỖ TRỢ THANH TOÁN</h4>
            <div className="flex flex-wrap gap-1.5">
       {['VNPAY', 'Momo', 'VISA', 'ATM', 'ZaloPay'].map(p => (
    <span key={p} className="text-xs px-2.5 py-1.5 rounded-lg font-bold"
               style={{ backgroundColor: '#2d1f0e', color: '#c8b09a', border: '1px solid #4a3020' }}>
     {p}
   </span>
              ))}
            </div>
   </div>
        </div>
      </div>

      {/* ── Bottom bar ──────────────────────────────────── */}
      <div style={{ borderTop: '1px solid #2d1f0e' }} className="py-4">
 <div className="max-w-7xl mx-auto px-4 flex flex-col md:flex-row items-center justify-between gap-2 text-xs" style={{ color: '#6a5040' }}>
   <span>© 2025 Sudes Craft. Tất cả quyền được bảo lưu.</span>
   <div className="flex items-center gap-3">
     <Link to="#" className="hover:text-amber-400">Chính sách bảo mật</Link>
   <span>·</span>
         <Link to="#" className="hover:text-amber-400">Điều khoản sử dụng</Link>
   </div>
     </div>
      </div>
    </footer>
  );
}
